# URL pública del sitio, usada para SEO (metadatos, Open Graph, sitemap, robots.txt).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# URL base de la API del backend NestJS, ej: https://api.nexomotor.com/v1
# Si se deja vacía, la app usa datos de ejemplo locales (src/lib/mock) para poder
# navegar el marketplace sin backend levantado.
API_URL=
